@model IEnumerable<WheelSell.Models.Make>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />

<div class="row">
    <div class="col-6">
        <h2 class="text-info">Makes of Vehicle</h2>
    </div>

    <div class="col-6">
        <a class="btn btn-info" asp-action="Create">
            Add New Make
        </a>
    </div>
    <br />
    <div class="col-12">
        <table class="table table-striped border">
            <tr class="table-info">
                <th>
                    @Html.DisplayNameFor(m=>m.Name)
                </th>
            </tr>
            @foreach(var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(m=>item.Name)
                    </td>
                    @*<td class="text-right">*@
                        @*
                        <a type="button" asp-action="Edit" asp-controller="Make">Edit</a>
                        <a type="button" asp-action="Delete" asp-controller="Make">Delete</a>*@
@*                    <div class="btn-group" role="group">

                        <a type="button" class="btn btn-primary" asp-action="Edits" asp-route-id="@item.Id" >Edit</a>

                        <form method="post" action="Delete">
                            
                        <input type="submit" class="btn btn-danger rounded-0" asp-route-id="@item.Id" asp-action="Delete" value="Delete"/>

                        </form>                       
                         </div>     
                    </td>*@

                    <partial name="_TableButtonPartial" model="item.Id"/>

                </tr>

            }
        </table>
    </div>
</div>